import Title from "./title";
import Content from "./content";

const Home = () => {
  return (
    <div className="inner">
      <Title lineContent="UNHCR × AMO" lineContent2="AMO App with GSAP" />
      <div>
        <div className="other">
          {/* Does project report used question death, out more rhetoric unpleasing
          what compared both of sentinels. */}
          <Content />
        </div>
      </div>
    </div>
  );
};

export default Home;
